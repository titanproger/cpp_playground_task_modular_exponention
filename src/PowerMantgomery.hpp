

template <typename TOperand, typename TExponent, typename TModulus>
constexpr int PowerMontgomery(TOperand x, TExponent exponent, TModulus modulus)
{    

    // TODO: use Mantgomery form to compute exponent faster
    // see https://en.wikipedia.org/wiki/Montgomery_modular_multiplication

    return 0;
}
